# Memory-Web
**Team:** *************** (data private in Moodle) <br>
**Repository:** https://github.com/Atomarverseucht/memory-web/
## Setup
Lokal mit:
```bash
npm install
npm run dev
```

## Kriterien

### React Router
| Kriterium                        | Datei                                   | Zeile / Hinweis                 |
|----------------------------------|-----------------------------------------|---------------------------------|
| 4 Routen, `<Link>`/`useNavigate` | src/client/App.tsx                      | Z. 10-15 – Routes mit Route     |
|                                  | src/client/main/Main.tsx                | Z. 11 – BrowserRouter           |
| Link zu Room                     | src/client/main/components/UIMemSet.tsx | Link zu /room/:randomId?memID=N |

### Datenfetching & REST
| Kriterium                                            | Datei                      | Zeile / Hinweis                                                   |
|------------------------------------------------------|----------------------------|-------------------------------------------------------------------|
| Fetch, GET                                           | src/server/app.ts          | Z. 30-65 – 4 Endpoints: GET /api/health, GET /api/memSets,        |
| + POST                                               | src/server/app.ts          | POST /api/register, POST /api/login                               |
| Sichtbarer Ladeindikator (Text)                      | src/client/main/client.tsx | Z. 10+30 – loading-State: `<p>Load...</p>`                        |
| Sichtbare Fehlermeldung bei 4xx/5xx / Netzwerkfehler | src/client/main/client.tsx | Z. 11+31 – error-State: `<p style={{color:"red"}}>Error: ...</p>` |

### Geteilter State (React Context)
| Kriterium             | Datei                                    | Zeile / Hinweis                   |
|-----------------------|------------------------------------------|-----------------------------------|
| Context-Definition    | src/client/room/state.tsx                | Z. 22 – UIContext (createContext) |
| Provider mit useState | src/client/room/state.tsx                | Z. 30-41 – UIProvider             |
| Konsum in RoomPage    | src/client/room/room.tsx                 | Z. 20 – useUIState()              |
| Konsum in Board       | src/client/room/components/board.tsx     | Z. 10 – useUIState()              |
| Konsum in PlayerBar   | src/client/room/components/playerBar.tsx | Z. 6 – useUIState()               |

### Tests
| Kriterium                    | Datei                       | Zeile / Hinweis                        |
|------------------------------|-----------------------------|----------------------------------------|
| 4 Tests (Vitest + Supertest) | src/server/test/api.test.ts | Z. 15-45                               |
| GET /api/health              | src/server/test/api.test.ts | Z. 16-20 – Status 200, `{status:"ok"}` |
| GET /api/memSets             | src/server/test/api.test.ts | Z. 22-27 – Gibt Set-Titel zurück       |
| POST /api/register           | src/server/test/api.test.ts | Z. 29-37 – Token + User bei Erfolg     |
| POST /api/login              | src/server/test/api.test.ts | Z. 39-45 – 401 bei falschen Daten      |

### Backend (Express)
| Kriterium             | Datei                | Zeile / Hinweis                                           |
|-----------------------|----------------------|-----------------------------------------------------------|
| Express-App           | src/server/app.ts    | Z. 21 – app = express()                                   |
| HTTP-Server + Port    | src/server/server.ts | createServer, listen(PORT)                                |
| REST-Endpoints        | src/server/app.ts    | Z. 30-65 – GET /api/health, GET /api/memSets,             |
|                       |                      | POST /api/register, POST /api/login                       |
| WebSocket (Socket.IO) | src/server/app.ts    | Z. 25-27 + 67-87 – wss.on("connection") mit Events        |
| Raum-Verwaltung       | src/server/room.ts   | initUser, onMessage, broadcast, exitUser                  |
| Spiellogik            | src/server/game.ts   | Board-Erstellung, Karten aufdecken, Paare prüfen, Timeout |

### Datenbank
| Kriterium           | Datei                  | Zeile / Hinweis                                                  |
|---------------------|------------------------|------------------------------------------------------------------|
| SQLite (persistent) | src/server/database.ts | Z. 1-32 – users-Tabelle (id, name, passwordHash), bcrypt-Hashing |

### Authentifizierung
| Kriterium                                      | Datei              | Zeile / Hinweis                            |
|------------------------------------------------|--------------------|--------------------------------------------|
| Login/Registrierung, JWT, geschützter Endpunkt | src/server/app.ts  | Z. 30-55 – POST /api/register & /api/login |
| JWT-Verifikation für WebSocket                 | src/server/auth.ts | Z. 9-17 – authenticateSocket()             |
| JWT-Secret via env                             | src/server/app.ts  | Z. 17 – JWT_SECRET                         |

### Architektur

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           CLIENT (Vite SPA)                                 │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │  src/client/Main.tsx (BrowserRouter)                                 │   │
│  │  src/client/App.tsx (Routes)                                         │   │
│  │                                                                      │   │
│  │  ┌──────────┐  ┌───────────────┐  ┌──────────┐  ┌───────────────┐   │   │
│  │  │  HomePage │  │  Room         │  │  Login   │  │  Register     │   │   │
│  │  │  /        │  │  /room/:id    │  │  /login  │  │  /register    │   │   │
│  │  └────┬─────┘  └──────┬────────┘  └────┬─────┘  └──────┬────────┘   │   │
│  │       │               │                 │               │            │   │
│  │       │        ┌──────┴──────┐          │               │            │   │
│  │       │        │  UIProvider │          │               │            │   │
│  │       │        │ (Context)   │          │               │            │   │
│  │       │        └──────┬──────┘          │               │            │   │
│  │       │               │                 │               │            │   │
│  │  ┌────┴─────┐   ┌─────┴──────────────┐  │               │            │   │
│  │  │UIMemSet  │   │  Board │ PlayerBar │  │               │            │   │
│  │  │codeSec   │   │  NameSection       │  │               │            │   │
│  │  └────┬─────┘   └─────┬──────────────┘  │               │            │   │
│  │       │               │                 │               │            │   │
│  │       └───────┬───────┘                 │               │            │   │
│  │               │                         │               │            │   │
│  │      ┌────────┴────────┐                │               │            │   │
│  │      │  connService    │◄──── Socket.IO ────────────────┼────────────┼───┤
│  │      │  (Socket.IO)    │                │               │            │   │
│  │      └────────┬────────┘                │               │            │   │
│  │               │                         │               │            │   │
│  │               ├── fetch /api/memSets ────┼───────────────┼────────────┼───┤
│  │               ├── fetch /api/login   ────┼───────────────┼────────────┼───┤
│  │               └── fetch /api/register────┼───────────────┼────────────┼───┤
│  └──────────────────────────────────────────┼───────────────┼────────────┼───┘
│                                             │               │            │
├─────────────────────────────────────────────┼───────────────┼────────────┼───┤
│               SERVER (Express + Socket.IO)  │               │            │   │
│  ┌──────────────────────────────────────────┼───────────────┼────────────┼───┘
│  │  src/server/app.ts                       │               │            │
│  │                                          │               │            │
│  │  ┌─────────────────────┐                 │               │            │
│  │  │  REST-Endpoints     │◄────────────────┼───────────────┼────────────┘
│  │  │  GET  /api/health   │                 │               │
│  │  │  GET  /api/memSets  │                 │               │
│  │  │  POST /api/register │                 │               │
│  │  │  POST /api/login    │                 │               │
│  │  └─────────────────────┘                 │               │
│  │                                          │               │
│  │  ┌─────────────────────┐                 │               │
│  │  │  WebSocket (wss)    │◄────────────────┼───────────────┘
│  │  │  Socket.IO Server   │                 │
│  │  └────────┬────────────┘                 │
│  │           │ initUser / onMessage         │
│  │  ┌────────┴────────────┐                 │
│  │  │  Room               │                 │
│  │  │  - users: Player[]  │                 │
│  │  │  - sockets: Socket[]│                 │
│  │  │  - broadcast()      │                 │
│  │  └────────┬────────────┘                 │
│  │           │ openField(clientID, index)   │
│  │  ┌────────┴────────────┐                 │
│  │  │  Game               │                 │
│  │  │  - BoardUI (64)     │                 │
│  │  │  - Karten-matching  │                 │
│  │  │  - Timeout 3s       │                 │
│  │  └─────────────────────┘                 │
│  │                                          │
│  │  ┌─────────────────────┐                 │
│  │  │  Auth               │                 │
│  │  │  - JWT sign/verify  │                 │
│  │  │  - authenticateSocket                │
│  │  └────────┬────────────┘                 │
│  │           │                              │
│  │  ┌────────┴────────────┐                 │
│  │  │  Database (SQLite)  │                 │
│  │  │  - users-Tabelle    │                 │
│  │  │  - bcrypt-Hashing   │                 │
│  │  └─────────────────────┘                 │
│  └──────────────────────────────────────────┘
│
└─────────────────────────────────────────────────────────────────────────────┘

### Datenfluss (Memory-Spiel)

1. Spieler öffnet `/` → HomePage fetcht `GET /api/memSets`
2. Klick auf ein Theme → navigiert zu `/room/:randomId?memID=N`
3. Room baut Socket.IO-Verbindung auf (`connService`)
4. Server: `connection` → `authenticateSocket()` → `Room.initUser()`
5. `initUser` sendet initialen State (`board`, `users`, `ownId`) via Socket
6. Klick auf Karte → `connService.sendMessage({cmd:"open", param:index})`
7. Server: `Game.openField()` matcht Paare, broadcastet neues Board
8. Bei Fehlpaar: 3s Timeout, dann Karten wieder schließen
9. `PlayerBar` zeigt aktuelle Spieler und Punktestände